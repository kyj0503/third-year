<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SNS 서비스</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="/css/main.css" rel="stylesheet">
</head>
<body class="light-mode">
<div class="container-fluid">
    <!-- 모드 전환 버튼 -->
    <div class="position-absolute top-0 end-0 p-3">
        <button id="toggle-mode" class="btn btn-secondary">다크 모드</button>
    </div>

    <div class="row vh-100">
        <!-- 로그인/회원가입 섹션 -->
        <div class="col-md-2 bg-light login-section">
            <h2>회원가입/로그인</h2>
            <form action="/login" method="post" class="w-75">
                <div class="mb-3">
                    <label for="username" class="form-label">아이디</label>
                    <input type="text" class="form-control" id="username" name="username" required>
                </div>
                <div class="mb-3">
                    <label for="password" class="form-label">비밀번호</label>
                    <input type="password" class="form-control" id="password" name="password" required>
                </div>
                <button type="submit" class="btn btn-primary w-100">로그인</button>
            </form>
            <a href="/signup" class="mt-3">회원가입</a>
        </div>

        <!-- 모든 포스트 섹션 -->
        <div class="col-md-5 bg-white post-section">
            <h2 class="text-center my-3">모든 포스트</h2>
            {{#posts}}
                <div class="border-bottom p-3">
                    <h5>{{username}}</h5> <!-- DTO에서 username 필드에 접근 -->
                    <p>{{content}}</p>
                    <small class="text-muted">{{createdAt}}</small>
                </div>
            {{/posts}}
            {{^posts}}
                <p class="text-center text-muted">포스트가 없습니다.</p>
            {{/posts}}
        </div>

        <!-- 글 작성 섹션 -->
        <div class="col-md-5 bg-light write-section">
            <h2>글 작성</h2>
            <form action="/post" method="post" class="w-75">
                <div class="mb-3">
                    <label for="content" class="form-label">내용</label>
                    <textarea class="form-control" id="content" name="content" rows="5" required></textarea>
                </div>
                <button type="submit" class="btn btn-success w-100">작성</button>
            </form>
        </div>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
<script src="/js/main.js"></script>
</body>
</html>

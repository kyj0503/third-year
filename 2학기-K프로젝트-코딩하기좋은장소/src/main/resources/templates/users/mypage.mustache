<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>마이페이지</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
<div class="container my-5">
    <h1 class="mb-4">마이페이지</h1>

    <!-- 유저 정보 수정 폼 -->
    <h2>회원 정보 수정</h2>
    <form action="/users/update" method="post">
        <div class="mb-3">
            <label for="username" class="form-label">아이디</label>
            <input type="text" id="username" name="username" class="form-control" value="{{user.username}}" required>
        </div>
        <div class="mb-3">
            <label for="password" class="form-label">비밀번호</label>
            <input type="password" id="password" name="password" class="form-control" value="{{user.password}}" required>
        </div>
        <div class="mb-3">
            <label for="email" class="form-label">이메일</label>
            <input type="email" id="email" name="email" class="form-control" value="{{user.email}}" required>
        </div>
        <button type="submit" class="btn btn-primary">수정</button>
    </form>

    <!-- 작성한 리뷰 리스트 -->
    <h2 class="mt-5">내가 작성한 리뷰</h2>
    {{#reviews}}
        <div class="card mb-3">
            <div class="card-body">
                <h5 class="card-title">{{place.name}}</h5>
                <p class="card-text">평점: {{rating}}점</p>
                <p class="card-text">댓글: {{comment}}</p>
                <p class="card-text"><small class="text-muted">작성일: {{createdAt}}</small></p>
            </div>
        </div>
    {{/reviews}}
    {{^reviews}}
        <p>작성한 리뷰가 없습니다.</p>
    {{/reviews}}

    <!-- 내가 즐겨찾기한 장소 -->
    <h2 class="mb-3">내가 즐겨찾기한 장소</h2>
    {{#favorites}}
        <div class="card mb-3">
            <div class="card-body">
                <h5 class="card-title">장소: {{place.name}}</h5>
                <p class="card-text">주소: {{place.address}}</p>
            </div>
        </div>
    {{/favorites}}
</div>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
